// Auto-generated stub for @betterat/ui/components/landing/ScrollFix
import React from 'react';
import { View, Text } from 'react-native';

const createStubComponent = (label: string) => {
  const Stub = (props: any) => {
    console.warn('[Stub] @betterat/ui/components/landing/ScrollFix -> ' + label, props);
    // ScrollFix is likely a utility component, just render children
    return props.children || null;
  };
  return Stub;
};

export type ScrollFix = any;
export type ScrollFixProps = any;
export const ScrollFix: any = createStubComponent('ScrollFix');

