// @ts-nocheck
// Auto-generated stub for @betterat/ui/components/races/LiveRaceTimer
import React from 'react';
import { View, Text } from 'react-native';

const createStubComponent = (label: string) => {
  const Stub = (props: any) => {
    console.warn('[Stub] @betterat/ui/components/races/LiveRaceTimer -> ' + label, props);
    return (
      <View accessibilityLabel={label}>
        <Text>{label} - Stub</Text>
      </View>
    );
  };
  return Stub;
};

export type LiveRaceTimer = any;
export type LiveRaceTimerProps = any;
export const LiveRaceTimer: any = createStubComponent('LiveRaceTimer');

