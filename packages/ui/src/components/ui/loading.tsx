// Auto-generated stub for @betterat/ui/components/ui/loading
import React from 'react';
import { View, Text } from 'react-native';

const createStubComponent = (label: string) => {
  const Stub = (props: any) => {
    console.warn('[Stub] @betterat/ui/components/ui/loading -> ' + label, props);
    return (
      <View accessibilityLabel={label}>
        <Text>{label} - Stub</Text>
      </View>
    );
  };
  return Stub;
};

export type DashboardSkeleton = any;
export type DashboardSkeletonProps = any;
export const DashboardSkeleton: any = createStubComponent('DashboardSkeleton');

export type loading = any;
export type loadingProps = any;
export const loading: any = createStubComponent('loading');

